<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.avramescu.codechallenges">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".week5.SecondActivityButtons"></activity>
        <activity android:name=".week5.ActivityButtons" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".week5.SecondActivity" />
        <activity android:name=".week5.FirstActivity">

        </activity>
        <activity android:name=".week4.RecyclerViewActivity" />
        <activity android:name=".week4.ConstraintLayout" />
        <activity android:name=".week4.ViewGroupsActivity" />
        <activity android:name=".week3.Spinner" />
        <activity android:name=".week3.FrameLayout" />
        <activity android:name=".week3.ScrollView" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>